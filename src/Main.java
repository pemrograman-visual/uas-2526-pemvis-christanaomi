/*
 * TEMPLATE Main.java
 * Students must replace this file with their exported Java code from Flowgorithm.
 * For testing, you can implement a simple echo or example solution.
 */
import java.util.*;
public class Main {
    public static void main(String[] args) {
        // placeholder to avoid compilation error if student doesn't provide code
        // This program does nothing by default. Replace with exported code from Flowgorithm.
        Scanner sc = new Scanner(System.in);
        // read all input and exit
        while (sc.hasNextLine()) {
            sc.nextLine();
        }
    }
}
